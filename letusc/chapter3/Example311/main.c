#include <stdio.h>
#include<stdlib.h>
/*
Write a program to find the absolute value of a number entered
through the keyboard*/
int main()
{
    int a;
    printf("enter the number");
    scanf("%d",&a);
    printf("absolute value of a is %d",abs(a));
    return 0;
}
